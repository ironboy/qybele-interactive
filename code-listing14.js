function start() {
  let $ = jQuery;
  let content = $(document.currentScript).parents('#js-module_content section.module_content');
  if (!content.length) { setTimeout(start, 20); console.log('retry'); return; }
  content.append('<p>This is some extra content generated by JavaScript.</p>');
}

start();
